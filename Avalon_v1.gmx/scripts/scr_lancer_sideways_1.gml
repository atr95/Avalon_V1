//Checkerboard Pattern. middle row.

var x_val = -128; // Random choice that leaves room for another lancer to its right.
var y_val = 128;
    instance_create(x_val, y_val, obj_lancer_2_a);
global.enemies += 1;
